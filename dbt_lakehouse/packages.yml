packages:
  # Fetched by `dbt deps` into dbt_packages/ (gitignored). The Airflow DAG runs
  # `dbt deps` as its first task, so a fresh clone works without manual steps.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
